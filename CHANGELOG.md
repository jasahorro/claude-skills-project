# Changelog — claude-skills-project

## Commit History

- `c7ac83e` feat: add greeting app (8 minutes ago)

## Working Tree Status

Clean. There are no uncommitted changes.

## Tracked Files

- `app.py`: A small Python script with a `greet(name)` function that returns a welcome message and an `add(a, b)` function that returns the sum of two numbers. When run directly, it prints both results.
